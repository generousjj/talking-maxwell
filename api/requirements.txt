# Vercel Python runtime — dependencies for api/index.py only.
# Keep in sync with [project.dependencies] in pyproject.toml (Vercel prefers
# that file when it exists). numpy/aiohttp/etc. from the local operator
# build are not needed here.

fastapi>=0.115,<1
httpx>=0.27,<1
python-dotenv>=1.0,<2
PyYAML>=6.0,<7
